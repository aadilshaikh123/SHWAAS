# Runtime dependencies only - this file is what Vercel installs into the function,
# so anything added here counts against the 250MB bundle limit.
# ML training deps (pandas, scikit-learn, numpy) live in ML/requirements_ml.txt.

# FastAPI and server dependencies
fastapi==0.104.1
pydantic==2.5.0

# HTTP requests - also how we call Gemini and Groq, so no vendor SDKs are needed
requests==2.31.0

# Tavily API for web search
tavily-python>=0.5.0

# Environment variables
python-dotenv==1.0.0

# Local development server only; Vercel provides its own.
uvicorn[standard]==0.24.0
